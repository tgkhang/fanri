# samples/private

Put real infra repos here (company code, client landing zones, ...) to test fanri locally.

Everything in this folder except this README is **gitignored**, so it never gets committed (doc 6 §6.9, "Real-world" testing).
